# ── Web 主应用运行所需 ─────────────────────────────────────────────────
# main.py / Module/ / Sqls/storage.py 用到
Flask>=3.1.0
Werkzeug>=3.0.0
Jinja2>=3.1.0
itsdangerous>=2.2.0
MarkupSafe>=3.0.0
click>=8.1.0
blinker>=1.9.0
Markdown>=3.10.2

# ── 生产 WSGI 服务器（Windows 部署用 wsgi.py 启动）─────────────────────
waitress>=3.0.0

# ── service/ 独立脚本依赖（不跑 service/ 可不装）──────────────────────
# service/kline.py   —— Gate.io 行情拉取 + MongoDB 写入
# service/run_road.py —— Redis/MongoDB 工具
# service/req_create.py —— HTTP 压测，仅用标准库
requests>=2.32.0
pymongo>=4.10.0
redis>=5.0.0

# ── 遗留：Sqls/pymysql.py 已不被 main.py 引用，如确认无用可删 ────────
PyMySQL>=1.1.0
